# Frequently Used Regex Patterns

### Validate Numbers Only

```
/^[0-9]+$/;
```

### Validate Emails

```
/^(([^<>()\[\]\.,;:\s@\"]+(\.[^<>()\[\]\.,;:\s@\"]+)*)|(\".+\"))@(([^<>()[\]\.,;:\s@\"]+\.)+[^<>()[\]\.,;:\s@\"]{2,})$/i;
```
